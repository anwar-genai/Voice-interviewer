fastapi>=0.112,<1
uvicorn[standard]>=0.30,<1
requests>=2.32,<3
beautifulsoup4>=4.12,<5
pdfplumber>=0.11,<1
pydantic>=2.13.4,<3
pydantic-settings>=2.4,<3
python-dotenv>=1.0,<2
SQLAlchemy>=2.0,<3
alembic>=1.13,<2
psycopg[binary]>=3.2,<4
PyJWT[crypto]>=2.9,<3

# Voice + Agents (optional at runtime for LiveKit agent)
livekit-agents[openai,silero,deepgram,cartesia,turn-detector]~=1.2
livekit-plugins-noise-cancellation~=0.2
cerebras-cloud-sdk>=1.2,<2

# CORS if needed later
starlette>=0.37,<1
python-multipart>=0.0.9

# Error tracking (active only when SENTRY_DSN is set)
sentry-sdk>=2,<4

